<?xml version="1.0" encoding="UTF-8"?>
<Command>
  <aliases l="en"></aliases>
  <aliases l="ru">алиас псевдоним</aliases>
  <aliases l="ua"></aliases>
  <cat>client</cat>
  <extra>keep_hide ghost afk</extra>
  <help aka="MACROS TRIGGERS МАКРОСЫ ТРИГГЕРЫ" id="1345" level="-1" ref="unalias" title="{CНастройки: {xсинонимы, составные команды, триггеры" type="CommandHelp">Команда *%CMD%* служит для сокращения длинных или составных команд. 
Использование синонимов (алиасов) очень удобно для быстрого набора команд или
последовательностей команд, которые ты часто используешь.

Во встроенном {hh2133веб-клиенте{x можно также настроить переменные, макросы и триггеры.

{CИСПОЛЬЗОВАНИЕ{x
%FMT%  *%CMD%*  
Показывает список всех твоих синонимов.

%FMT%  *%CMD%* _слово_
Показывает синоним для этого слова, если таковой имеется.

%FMT%  *%CMD%* _слово_ _подстановка_
Создает новый синоним.

%FMT%  *%CMD%* *(flush,очистить)*
Удаляет все синонимы.

%FMT%  *(unalias,несиноним)* _слово_
Удаляет ненужный синоним.

{CОГРАНИЧЕНИЯ{x
Сокращенная команда не может содержать других сокращений, но может выполнять
несколько команд, если их перечислить через *|*. Вокруг символа пайпа нельзя
ставить пробелы.

Слово во введенной строке будет заменено, только если оно стоит в самом начале.
Команды *%CMD%* и *(unalias,несиноним)* нельзя присвоить синонимам.
 
{CПРИМЕРЫ{x
%CMD% {lRвт взять все труп{lEgc get all corpse{lx
-- после этого, написание {lRвт{lEgc{lx будет эквивалентно {lRвзять все труп{lEget all corpse{lx
%CMD% {lRмолн колд &apos;удар молнии&apos;{lElb cast &apos;lightning bolt&apos;{lx
-- {lRмолн орк{lElb orc{lx будет расшифровано как {lRколд &apos;удар молнии&apos; орк{lEcast &apos;lightning bolt&apos; orc{lx
%CMD% {lRобед откр сумка || взять пирог сумка || есть пирог || закрыть сумка{lEdinner open bag || take pie bag | eat pie | close bag{lx
-- при вводе {lRобед{lEdinner{lx будут последовательно выполнены эти четыре команды.

%SA% %H% [(command separator,разделитель команд)]
</help>
  <hint l="en"></hint>
  <hint l="ru">задать синоним для команд</hint>
  <hint l="ua"></hint>
  <name l="en">alias</name>
  <name l="ru">синоним</name>
  <name l="ua"></name>
  <order>player_only</order>
  <position>dead</position>
</Command>
