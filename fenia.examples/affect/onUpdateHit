.AffectHandler(@name@).@trig@ = function(ch, af) {
    /* Called every second at the beginning of char hit points update.
       Return true if you want to interrupt normal HP update.
     */
    return false;
}
