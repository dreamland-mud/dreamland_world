.Behavior(@name@).@trig@ = function(mob, ch, stat) {
    // Invoked after succesful train for stat ("str", "wis" and so on)
    if (stat == "str") {
    	mob.say("Вау, теперь ты гораздо сильнее!");
         
    }
    return true;
}

