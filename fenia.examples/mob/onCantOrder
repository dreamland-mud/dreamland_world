.get_mob_index(@vnum@).@trig@ = function(mob, master, cmdName, cmdArgs) {
    // Вызывается после того, как успешно прошли все проверки, но до выполнения мобом команды cmdName с аргументами.
    // Вернуть true, если приказ надо прервать.

    return false;
}

