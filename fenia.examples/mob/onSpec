.get_mob_index(@vnum@).@trig@ = function(mob) {
    // Кот, который периодически мяукает.
    if (.chance(1))
        mob.interpret("эмоц громко и протяжно мяукает.");

    // Вернуть false, чтобы продолжили выполняться другие стандартные действия для мобов.
    return false;
}

