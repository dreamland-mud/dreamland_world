.get_obj_index(@vnum@).@trig@ = function(obj, ch) {
    // Пример простого вывода сообщения при надевании вещи.
    // Аффекты вешаются в триггере onEquip.
    ch.act("Приятная тяжесть %O2 вселяет в тебя уверенность в завтрашнем дне.", obj);
}

