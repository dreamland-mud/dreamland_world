.get_room_index(@vnum@).@trig@ = function(walker, fromRoom, moveType) {
    // Walker: who just entered this room
    // fromRoom: where they came from
    // moveType: one of normal, swimming, flee, riding, flying, running, waterwalking, slink, crawl, quickly
}

