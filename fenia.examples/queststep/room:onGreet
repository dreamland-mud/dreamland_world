.get_room_index(@vnum@).@trig@ = function(ch, fromRoom, moveType) {
    // ch: who just entered this room
    // fromRoom: where they came from
    // moveType: one of normal, swimming, flee, riding, flying, running, waterwalking, slink, crawl, quickly
    if (.tmp.quest.active(ch, "@quest.vnum@", @quest.step@)) {
    }
}

