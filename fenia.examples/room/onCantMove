.get_room_index(@vnum@).@trig@ = function(walker, targetRoom, moveType) {
    // walker: who tries to move out of this room
    // targetRoom: where they intend to go
    // moveType: one of normal, swimming, flee, riding, flying, running, waterwalking, slink, crawl, quickly


    // Return true to forbid leaving this room.
    return false;
}
