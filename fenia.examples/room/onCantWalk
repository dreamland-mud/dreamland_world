.get_room_index(@vnum@).@trig@ = function(walker, extraExit) {
    // walker: who tries to leave this room via an extra exit
    // extraExit: extra exit name

    // Return true to prevent walker from leaving this room.
    return false;
}

