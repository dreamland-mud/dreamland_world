.get_room_index(@vnum@).@trig@ = function(actor, cmdName, cmdArgs) {
    // An actor tries to execute an existing command 'cmdName' with arguments.

    // Return true here to prevent normal command execution.
    return false;
}

