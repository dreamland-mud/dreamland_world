.get_room_index(@vnum@).@trig@ = function(actor, skill, success, victim) {
    // actor: who used skill in this room
    // skill name
    // success: true for successful skill, false for failure
    // victim: skill target or null
    
}

