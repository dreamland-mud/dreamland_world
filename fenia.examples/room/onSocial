.get_room_index(@vnum@).@trig@ = function(actor, victim, social, arg) {
    // actor: who did the social
    // victim: social target or null
    // social: social name
    // arg: social argument
}

