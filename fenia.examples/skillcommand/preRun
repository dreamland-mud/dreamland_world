.SkillCommand(@name@).preRun = function() {
    // Триггер preRun срабатывает сразу после разбора аргументов и ДО того,
    // как будут списаны мана и шаги и назначена задержка. В run все это уже
    // потрачено -- поэтому проверки "а можно ли вообще применить умение"
    // должны жить здесь, иначе отказ обойдется игроку в полную стоимость.
    //
    //   return false -- умение отменяется, не потратив ничего.
    //   return true (или ничего) -- умение выполняется дальше, как обычно.
    //
    // Прием ch.wait = 0 здесь не нужен: задержки еще нет.
    //
    // Сюда переносятся только чистые проверки: ничего не менять, не бросать
    // кубики и не слать сообщения в комнату. Кубики и все, что меняет мир,
    // остаются в run -- проигранный бросок обязан стоить игроку шагов.

    /*
    if (ch.flying) {
        ch.act("Для использования этого умения нужно приземлиться.");
        ch.echoMaster("Для использования этого умения нужно приземлиться.");
        return false;
    }

    if (ch.isAffected(skill.name)) {
        ch.act("Ты уже это делаешь.");
        return false;
    }
    */

    return true;
}
