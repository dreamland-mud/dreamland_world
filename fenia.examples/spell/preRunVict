.Spell(@name@).@trig@ = function() {
    // Триггер @trig@ срабатывает после выбора цели и ДО того, как будет
    // назначена задержка, произнесены слова заклинания, выставлен флаг
    // агрессии и списана мана. В runVict все это уже случилось, а следом
    // заклинание еще и прокачивается -- поэтому проверки "а можно ли вообще
    // это сотворить" должны жить здесь.
    //
    //   return false -- заклинание отменяется, не потратив ничего.
    //   return true (или ничего) -- заклинание творится дальше, как обычно.
    //
    // Прием ch.wait = 0 здесь не нужен: задержки еще нет.
    //
    // Внимание: level здесь -- это уровень персонажа, а не итоговый уровень
    // заклинания. Настоящий уровень считается позже, и посчитать его раньше
    // нельзя: заодно с ним бросается и прокачивается "магическое творчество".
    //
    // Сюда переносятся только чистые проверки: никаких кубиков, изменений
    // состояния и сообщений в комнату -- все это остается в runVict.

    /*
    if (ch.isAffected("fatigue")) {
        ch.act("Тебе надо подождать, прежде чем творить это заклинание снова.");
        return false;
    }

    if (vict.isAffected(skill.name)) {
        ch.act("На %1$C4 это заклинание уже наложено.", vict);
        return false;
    }
    */

    return true;
}
